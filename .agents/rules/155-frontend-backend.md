# Frontend ↔ Backend

When a frontend exists, verify the complete path rather than screen rendering. Confirm API base URL, environment variables, proxy/ports, CORS, credential mode, cookies, Authorization headers, request schema, response parsing, and client error handling.

Use browser/network or equivalent runtime evidence when practical. A successful UI render is not evidence that the backend request succeeded or that real persistence occurred.