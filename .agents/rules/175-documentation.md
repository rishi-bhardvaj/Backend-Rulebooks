# Documentation

Document public API contracts, security assumptions, operational configuration, migrations, external dependencies, and non-obvious failure behavior.

Documentation must match executable behavior. Update it when a public contract or operational procedure changes. Never document a feature as supported solely because code exists; distinguish implemented from verified where appropriate.